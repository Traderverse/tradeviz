% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/live.R
\name{plot_live}
\alias{plot_live}
\title{Live Streaming Chart}
\usage{
plot_live(
  data_function,
  chart_type = "candles",
  update_interval = 5000,
  max_points = 500,
  title = NULL,
  theme_dark = TRUE,
  indicators = NULL,
  height = 600
)
}
\arguments{
\item{data_function}{Function that returns updated data (e.g., fetch_prices)}

\item{chart_type}{Type of chart: "candles", "line", "equity" (default: "candles")}

\item{update_interval}{Update interval in milliseconds (default: 5000 = 5 seconds)}

\item{max_points}{Maximum points to display (default: 500)}

\item{title}{Chart title (default: auto-generated)}

\item{theme_dark}{Use dark theme (default: TRUE for live charts)}

\item{indicators}{Optional list of indicator functions to apply}

\item{height}{Chart height in pixels (default: 600)}
}
\value{
A Shiny UI element with live chart
}
\description{
Create a live-updating chart that refreshes with new data automatically.
Uses Shiny reactivity for real-time updates. Ideal for monitoring live markets.
}
\examples{
\dontrun{
library(shiny)
library(tradeviz)
library(tradeio)

# Define data fetcher
get_live_data <- function() {
  fetch_prices("AAPL", from = Sys.Date() - 30)
}

# Create live chart
ui <- fluidPage(
  plot_live(get_live_data, chart_type = "candles", update_interval = 5000)
)

server <- function(input, output, session) {}
shinyApp(ui, server)
}
}
